# 成本监控与性能优化

> Agent 天然是"token 消耗大户"。一个循环 5 步的 Agent 每次调用可能消耗几千甚至上万 token。没有成本监控，月底的账单会让你措手不及。

## 目录

- [Agent 成本模型](#agent-成本模型)
- [成本核算体系](#成本核算体系)
- [性能指标体系](#性能指标体系)
- [优化策略](#优化策略)
- [预算管控](#预算管控)
- [监控告警](#监控告警)
- [实践案例](#实践案例)
- [总结](#总结)
- [参考链接](#参考链接)

你好，我是江小湖。上一篇文章解决了"出问题了怎么看"，但还有一个不可忽视的维度：**成本**。

一个生产环境的 Agent，每天处理 10 万请求、平均每次调用 3 次 LLM、每次 LLM 调用消耗 2000 token——一个月就是 18 亿 token。按月账单算下来，可能是五位数甚至六位数人民币。**没有监控的成本，迟早会让你惊讶**。

## Agent 成本模型

Agent 的成本比传统 API 调用复杂得多。一个请求可能触发多次 LLM 调用、多次工具调用、多次检索。

### 成本构成

<p align="center">
  <img src="../../assets/13-observability/cost-breakdown.svg" alt="Agent 成本构成" width="95%"/>
</p>

| 成本项 | 典型占比 | 影响因素 |
|--------|---------|---------|
| LLM 推理 | 70-85% | 模型价格、token 量、调用次数 |
| 向量检索 | 5-15% | 文档量、检索频次、索引类型 |
| 工具调用 | 3-10% | 外部 API 单价、调用频次 |
| 基础设施 | 2-5% | 服务器、存储、网络 |

### 成本公式

```
单次 Agent 请求成本 =
  Σ(每次 LLM 调用 (input_tokens × input_price + output_tokens × output_price))
  + Σ(每次检索 (检索计费单位 × 单价))
  + Σ(每次工具调用 (API 价格))
```

## 成本核算体系

### 核心指标

| 指标 | 定义 | 计算公式 |
|------|------|---------|
| 每次请求平均成本 | 一次 Agent 执行的总成本 | 总成本 / 请求数 |
| 每次 LLM 调用平均 token | 单次 LLM 的 token 消耗 | 总 token / LLM 调用次数 |
| 每请求 LLM 调用次数 | 平均触发几次 LLM | LLM 调用次数 / 请求数 |
| 输入输出比 | 输入 token 与输出 token 的比例 | input_tokens / output_tokens |
| 缓存命中率 | prompt 缓存减少的 token | 缓存节省 token / 总 token |

### 按维度聚合

**按模型聚合**：gpt-4o 花了多少钱？gpt-4o-mini 花了多少钱？

**按用户聚合**：哪些用户是"token 消耗大户"？他们的使用模式合理吗？

**按功能聚合**：功能 A 和功能 B，哪个成本更高？哪个 ROI 更高？

**按时间聚合**：每天的 token 消耗趋势如何？周末和上班日的成本差异大吗？

## 性能指标体系

### 关键性能指标

| 指标 | 基准值 | 告警阈值 |
|------|--------|---------|
| P50 响应时间 | < 2s | > 5s |
| P95 响应时间 | < 5s | > 10s |
| P99 响应时间 | < 10s | > 20s |
| 首次 token 时间 (TTFT) | < 500ms | > 2s |
| 错误率 | < 1% | > 5% |
| 重试率 | < 2% | > 10% |

### 成本-性能关联分析

```
             低成本         高成本
               │              │
  高性能    ┌───── Good ── Trade-off
  低性能    │  Review ────  Bad
```

- **Good**：高性能 + 低成本，理想状态
- **Trade-off**：高性能 + 高成本，可能用了过于昂贵的方案
- **Review**：低性能 + 低成本，可能在降本上过了头
- **Bad**：低性能 + 高成本，需要紧急优化

## 优化策略

### 模型路由

```
用户输入
  │
  ▼
分类器（规则 or 小型 LLM）
  ├── 简单任务 → gpt-4o-mini (成本降低 80-90%)
  ├── 复杂任务 → gpt-4o
  └── 特殊任务 → o4-mini
```

实践中，**60-70% 的请求可以用小型模型处理**，而用户感知不到差异。

### Prompt 压缩

- **精简 system prompt**：去掉冗余示例
- **对话历史压缩**：用摘要代替原始对话历史，或只保留最近 N 轮
- **检索压缩**：检索内容只保留最相关的片段而不是整篇文档
- **结构化提示**：用 JSON/XML 替代自然语言描述

### 缓存策略

| 缓存层次 | 缓存对象 | 典型命中率 | 效果 |
|---------|---------|-----------|------|
| Prompt 层 | 相同 system prompt 的 KV Cache | 50-70% | 减少 TTFT |
| 响应层 | 相同输入对相同输出 | 10-30% | 完全避免 LLM 调用 |
| 检索层 | 相同 query 的检索结果 | 40-60% | 避免重复检索 |
| 工具层 | 相同 API 调用的结果 | 20-40% | 避免重复外部调用 |

### 批量处理

对于延迟不敏感的任务，用批量 API 替代实时 API。适合：内容审核、批量分类、数据标注、报告生成。

### 减少 LLM 调用次数

- **规则前置**：能用规则判断的，不交给 LLM
- **一次完成多件事**：合并多个简短 LLM 调用为一个复杂度更高的调用
- **有限状态机**：对于确定性流程，用有限状态机代替 LLM 循环

## 预算管控

### 预算分配模型

```
月度总预算
├── 模型 A 预算上限 (gpt-4o)
├── 模型 B 预算上限 (gpt-4o-mini)
├── 向量检索预算上限
└── 外部工具预算上限
```

### 预算保护机制

- **软限制**：超过 80% 预算时发送告警，服务自动使用更经济的模型
- **硬限制**：超过 100% 预算时，非核心功能降级或停止服务
- **每请求预算**：单个请求最多调用 10 次 LLM 或最多消耗 50,000 token

## 监控告警

| 告警类型 | 条件 | 响应 |
|---------|------|------|
| 预算预警 | 日消耗 > 日均预算 × 1.5 | 检查是否有异常流量 |
| 成本飙升 | 成本月环比增长 > 20% | 分析驱动因素 |
| Token 异常 | 单请求 token > 平均值 × 5 | 检查是否有死循环或过大上下文 |
| 模型偏斜 | 高成本模型占比 > 预期值 | 检查模型路由是否正常工作 |

## 实践案例

### 场景：客服 Agent

**状态**：日活 10 万用户，日均 30 万请求

**优化前**：所有请求调用 gpt-4o，平均每请求 3 次 LLM 调用，月成本约 ¥120,000

**优化后**：
1. 模型路由：60% 简单请求 → gpt-4o-mini
2. 缓存：常见问答缓存命中率 25%
3. Prompt 压缩：对话历史从 20 轮压缩到 8 轮 + 摘要
4. 死循环检测：同一工具连续调用 > 3 次时自动终止

**结果**：月成本 ¥120,000 → ¥43,000（节省 64%），P95 延迟 4.2s → 2.1s

## 总结

成本优化不是"省钱比赛"，而是在**成本、性能和用户体验之间找到平衡点**。

核心原则：先测量（每个请求的 token 和成本）→ 再设置基准 → 再优化（从影响最大的项开始）→ 持续监控。

**下一篇**：[监控体系](03-monitoring.md)——如何建立生产级的 Agent 监控仪表盘。

## 参考链接

- [OpenAI — Cost Optimization](https://platform.openai.com/docs/guides/cost-optimization)
- [Anthropic — Managing Costs](https://docs.anthropic.com/en/docs/managing-costs)
- [LangSmith — Cost Tracking](https://docs.smith.langchain.com/tracing/faq/managing_and_analyzing_tracing_data)
- [LangFuse — Cost Calculation](https://langfuse.com/docs/model-usage-and-cost)
- [Google — Gemini Cost Optimization](https://ai.google.dev/pricing)
